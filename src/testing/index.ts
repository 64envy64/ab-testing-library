/**
 * Public entry for testing helpers (`ab-testing-library/testing`).
 *
 * Exposes the deterministic mock remote-config transport used by tests and the
 * admin demo.
 */
export { createMockRemoteTransport, type MockRemoteTransport } from './mockRemoteConfig'
